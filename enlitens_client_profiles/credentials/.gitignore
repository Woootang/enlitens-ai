# Place Google service account JSON in this directory.
#
# The pipeline will look for `service_account.json` by default. Ensure the
# credentials file is not committed to version control.
*
!.gitignore

