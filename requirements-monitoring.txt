# Requirements for Enlitens AI Monitoring System

# FastAPI and WebSocket support
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0

# Existing requirements (if not already installed)
httpx==0.25.0
pydantic==2.4.2
json-repair==0.2.0
